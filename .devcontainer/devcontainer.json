{
  "name": "Python 3.13",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.13-bookworm",
  "customizations": {
    "codespaces": {
      "openFiles": [
        "README.md",
        "liturgie_app.py"
      ]
    },
    "vscode": {
      "settings": {},
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "updateContentCommand": "python -m pip install --upgrade pip && if [ -f packages.txt ]; then sudo apt update && sudo xargs apt install -y < packages.txt; fi && if [ -f requirements.txt ]; then python -m pip install --user -r requirements.txt; fi && echo '✅ Packages installed and requirements met'",
  "postAttachCommand": {
    "server": "streamlit run liturgie_app.py"
  },
  "portsAttributes": {
    "8501": {
      "label": "Application",
      "onAutoForward": "openPreview"
    }
  },
  "forwardPorts": [
    8501
  ]
}
